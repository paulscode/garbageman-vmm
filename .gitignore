# ----- OS / Editor junk -----
# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# Linux
*~
.directory

# IDEs
.vscode/
.idea/
*.code-workspace
*.iml
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.swp
*.swo
*.sublime-project
*.sublime-workspace
.project
.classpath
.settings/
.loadpath
.buildpath
nbproject/
.nb-gradle/

# ----- Backup files -----
*.backup
*.backup-*
*.bak
*.old

# ----- Shell / logs / temp -----
*.log
*.tmp
*.temp
tmp/
logs/

# ----- Env / secrets (if you ever add them) -----
.env
.env.*
!.env.example
*.secret
*.key
*.pem
*.crt
*.p12
*.pfx

# ----- Node.js / JavaScript / TypeScript -----
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
.pnpm-store/
*.tsbuildinfo
*.tgz
package-lock.json.bak

# Next.js
.next/
out/
.vercel

# Build outputs
dist/
build/
*.local

# Testing
coverage/
.nyc_output/
*.lcov

# TypeScript cache
*.tsbuildinfo
.tsbuildinfo

# ----- Build outputs (host-side Garbageman build) -----
# If you build Garbageman in the repo (default is $HOME/src/garbageman)
# Note: Only exclude root-level src/, not webui/*/src/
/src/
**/CMakeFiles/
**/CMakeCache.txt
**/cmake_install.cmake
**/Makefile

# ----- Runtime data / Instance files -----
# Instance configuration and data
envfiles/instances/
webui/.envfiles/
# But keep the envfiles directory structure and GLOBAL.env template
!envfiles/
!envfiles/GLOBAL.env
# Blockchain artifacts and binaries
.artifacts/
webui/.artifacts/
*.dat
*.blk

# Discovered peers data (runtime)
discovered-peers.json

# ----- Docker / Container artifacts -----
.dockerignore.bak
docker-compose.override.yml

# ----- Caddy reverse proxy data (TLS certificates, private keys) -----
# These are stored in Docker volumes by default, but protect against
# accidental commits if someone mounts host directories
caddy-data/
caddy-config/
.caddy/
caddy/data/
caddy/config/
# Caddy logs may contain IPs and request patterns
caddy/logs/
caddy.log
access.log
# Local Caddyfile overrides (if users add sensitive config)
Caddyfile.local
**/Caddyfile.local
*.caddy.local

# ----- Script caches & artifacts -----
# Binaries staged by the TUI for guest injection
.cache/gm-bins/
# Monitor’s temporary SSH key + known_hosts
.cache/gm-monitor/

# ----- ISOs / disk images / VM artifacts (never commit these) -----
*.iso
*.qcow2
*.raw
*.img
*.vmdk
*.vdi

# ----- Archive blobs -----
*.zip
*.tar
*.tar.gz
*.tgz
*.bz2
*.xz
*.7z

# ----- Misc tools -----
shellcheck-results/
coverage/
dist/
build/
