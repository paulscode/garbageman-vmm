# ==============================================================================
# GLOBAL CONFIGURATION - Garbageman Nodes Manager
# ==============================================================================
# Shared defaults for all daemon instances.
# Instance-specific settings in envfiles/instances/*.env override these.
#
# Last updated: 2025-11-05

# ------------------------------------------------------------------------------
# Bitcoin Implementation
# ------------------------------------------------------------------------------
# Which Bitcoin implementation to use by default.
# Options: garbageman | knots
BITCOIN_IMPL=garbageman

# ------------------------------------------------------------------------------
# Network
# ------------------------------------------------------------------------------
# Which Bitcoin network to connect to by default.
# Options: mainnet | testnet | signet | regtest
NETWORK=mainnet

# ------------------------------------------------------------------------------
# Data Directory
# ------------------------------------------------------------------------------
# Base directory where daemon data is stored.
# Each instance will create a subdirectory: ${BASE_DATA_DIR}/<instance-id>/
BASE_DATA_DIR=/data/bitcoin

# ------------------------------------------------------------------------------
# Network Exposure
# ------------------------------------------------------------------------------
# Whether to allow clearnet (IPv4/IPv6) P2P connections by default.
# 0 = Tor-only (default for privacy)
# 1 = Allow clearnet connections
EXPOSE_CLEARLY=0

# ------------------------------------------------------------------------------
# Resource Limits (Defaults)
# ------------------------------------------------------------------------------
# Default CPU and RAM allocations for new instances.
# Instances can override these in their own ENV files.
DEFAULT_CPUS=1
DEFAULT_RAM_MB=1024

# ------------------------------------------------------------------------------
# Pruning
# ------------------------------------------------------------------------------
# Enable pruning by default to save disk space.
# 0 = full node (keep all blocks)
# 1 = pruned node (keep only recent blocks, ~10GB)
DEFAULT_PRUNED=1

# ------------------------------------------------------------------------------
# Notes
# ------------------------------------------------------------------------------
# - Instance-specific settings go in envfiles/instances/<instance-id>.env
# - Port assignments are managed per-instance to avoid conflicts
# - TOR_ONION addresses are generated per-instance
